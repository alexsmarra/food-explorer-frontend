import styled from 'styled-components'

export const Container = styled.div`
   display: flex;
   align-items: center;
   gap: .8rem;
   cursor: pointer;

   img {
      width: 1.8rem;
      height: 1.8rem;
   }

   h1 {
      font-size: 1.4rem;
      font-family: 'Roboto', sans-serif;
   }
`
