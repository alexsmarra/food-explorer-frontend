import styled from "styled-components"

export const Container = styled.div`

display: flex;
flex-direction: column;
gap: 1rem;

img {
   width: 100px;
   height: 100px;
   border-radius: 50%;
   object-fit: cover;
} 
`